method: bayes  # or 'random' or 'grid'
metric:
  name: val_acc
  goal: maximize

parameters:
  learning_rate:
    min: 0.00001
    max: 0.01
  batch_size:
    values: [4, 8, 16]
  optimizer:
    values: ["adam", "sgd"]